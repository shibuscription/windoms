# Windoms Decisions

## TODOモジュール（DEMO）

- TODOは「担当1人 + 引き継ぐ（巻き取り）」で開始する。
- `dueDate` は任意で日付のみ（`YYYY-MM-DD`）。時刻は持たない。
- `related` は `0..1`（`event` または `session` のどちらか1つ）で扱う。
- メニューTODOは全体棚卸し、上部TODOアイコンは自分の受信箱とする。
